Para instalar el paquete:

1. Primero asegurarse de tener la ultima version de pip instalada, par ello ejecutamos
el siguiente código en nuestro terminal: python.exe -m pip install --upgrade pip

2. Luego de tener la ultima version instalada, ejecutar cd dist en la terminal.

3. Por ultimo, instalar el paquete ejecutando el siguiente comando en la terminal:

pip install mi_paquete-1.0.tar.gz
